version: '3.8'
services:
  api:
    build: .
    ports:
      - "3344:3344"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3344
    restart: unless-stopped
